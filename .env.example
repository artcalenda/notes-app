PORT=3000
DATABASE_PATH=./data/notes.db
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173

# Production: path to built frontend assets (optional)
# STATIC_DIR=./frontend/dist

# Frontend (development only; Vite proxy is used by default)
# VITE_API_URL=
